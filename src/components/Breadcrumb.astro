---
/**
 * Shared breadcrumb navigation. Consistent separator and spacing
 * across dashboard and admin pages.
 *
 * Items can be link objects ({ label, href }) or plain strings. The last
 * item is always rendered as the current page (non-link) regardless of
 * whether href is set.
 */

interface Item {
  label: string;
  href?: string;
}

interface Props {
  items: Item[];
  class?: string;
}

const { items, class: extraClass } = Astro.props;
---
<nav
  aria-label="Breadcrumb"
  class:list={['flex items-center gap-2 text-sm text-ink-soft', extraClass]}
>
  {items.map((item, i) => {
    const isLast = i === items.length - 1;
    return (
      <>
        {i > 0 && <span aria-hidden="true" class="text-ink-faint font-display">/</span>}
        {isLast || !item.href ? (
          <span class="text-ink" aria-current={isLast ? 'page' : undefined}>
            {item.label}
          </span>
        ) : (
          <a href={item.href} class="hover:text-ink transition-colors">
            {item.label}
          </a>
        )}
      </>
    );
  })}
</nav>
