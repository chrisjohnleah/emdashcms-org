# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production
.dev.vars

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# wrangler
.wrangler/

# planning (local-only, not for public repo)
.planning/

# editor settings
.vscode/

# design artifacts
.aidesigner/

# design context (local-only — internal direction, not for public repo)
.impeccable.md

# audit model benchmark — local-only workspace for scripts/audit-benchmark.ts
# Packed .tgz bundles are regenerated from bench-fixtures/sources/ on
# each run; result JSON dumps are noisy and per-run. The `sources/`
# directory IS tracked (see bench-fixtures/sources/README.md) so the
# benchmark is reproducible.
bench-fixtures/*.tgz
bench-results/
